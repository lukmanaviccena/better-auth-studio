export const Button = () => {
  return <button>Button</button>;
};

export const buttonConfig = {
  size: 'large',
  color: 'blue',
  variant: 'primary',
};

export const apiConfig = {
  basePath: '/api/auth',
  timeout: 5000,
};